/**
 * Sheet demo route — temporary entry point to test the MapSheet in isolation.
 *
 * Access via: npx expo start, then navigate to /sheet-demo
 * Or temporarily swap this into the tab navigator.
 *
 * Remove this file once the sheet is integrated into the main map view.
 */
export { default } from "@/components/map-sheet/sheet-demo";
