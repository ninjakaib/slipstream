// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AcceptFriendRequestData, AcceptFriendRequestErrors, AcceptFriendRequestResponses, AcceptJoinRequestData, AcceptJoinRequestErrors, AcceptJoinRequestResponses, ActivateCarData, ActivateCarErrors, ActivateCarResponses, AuthWithAppleData, AuthWithAppleErrors, AuthWithAppleResponses, CheckUsernameData, CheckUsernameErrors, CheckUsernameResponses, CreateCarData, CreateCarErrors, CreateCarResponses, CreateConvoyData, CreateConvoyErrors, CreateConvoyResponses, DebugSpatialStoreData, DebugSpatialStoreResponses, DeclineFriendRequestData, DeclineFriendRequestErrors, DeclineFriendRequestResponses, DeclineJoinRequestData, DeclineJoinRequestErrors, DeclineJoinRequestResponses, DeleteAccountData, DeleteAccountResponses, DeleteCarData, DeleteCarErrors, DeleteCarResponses, EndConvoyData, EndConvoyErrors, EndConvoyResponses, GetActiveRouteData, GetActiveRouteErrors, GetActiveRouteResponses, GetConvoyData, GetConvoyErrors, GetConvoyResponses, GetMessagesData, GetMessagesErrors, GetMessagesResponses, GetMyProfileData, GetMyProfileResponses, GetSpatialConfigData, GetSpatialConfigResponses, GetUserProfileData, GetUserProfileErrors, GetUserProfileResponses, HealthCheckData, HealthCheckResponses, InviteToConvoyData, InviteToConvoyErrors, InviteToConvoyResponses, JoinConvoyData, JoinConvoyErrors, JoinConvoyResponses, KickMemberData, KickMemberErrors, KickMemberResponses, LeaveConvoyData, LeaveConvoyErrors, LeaveConvoyResponses, ListCarsData, ListCarsResponses, ListFriendRequestsData, ListFriendRequestsResponses, ListFriendsData, ListFriendsResponses, LoginData, LoginErrors, LoginResponses, LogoutData, LogoutErrors, LogoutResponses, RefreshData, RefreshErrors, RefreshResponses, RegisterData, RegisterErrors, RegisterResponses, RemoveFriendData, RemoveFriendErrors, RemoveFriendResponses, RequestToJoinData, RequestToJoinErrors, RequestToJoinResponses, SearchUsersData, SearchUsersErrors, SearchUsersResponses, SendFriendRequestData, SendFriendRequestErrors, SendFriendRequestResponses, SendMessageData, SendMessageErrors, SendMessageResponses, SendQuickActionData, SendQuickActionErrors, SendQuickActionResponses, SetRouteData, SetRouteErrors, SetRouteResponses, UpdateCarData, UpdateCarErrors, UpdateCarResponses, UpdateConvoyData, UpdateConvoyErrors, UpdateConvoyResponses, UpdateMyProfileData, UpdateMyProfileErrors, UpdateMyProfileResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Register
 *
 * Create a new user account and return tokens.
 *
 * Signup collects only email + password; the username is a temp handle the
 * user replaces during onboarding (unless a client explicitly supplies one).
 */
export const register = <ThrowOnError extends boolean = false>(options: Options<RegisterData, ThrowOnError>) => (options.client ?? client).post<RegisterResponses, RegisterErrors, ThrowOnError>({
    url: '/auth/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Auth With Apple
 *
 * Exchange Apple identity token for SlipStream tokens.
 *
 * This endpoint:
 * 1. Fetches Apple's JWKS (cached for 1 hour)
 * 2. Validates the identity token signature and claims
 * 3. Finds or creates a user by apple_id
 * 4. Issues access and refresh tokens
 * 5. Returns is_new_user=true for first-time users
 */
export const authWithApple = <ThrowOnError extends boolean = false>(options: Options<AuthWithAppleData, ThrowOnError>) => (options.client ?? client).post<AuthWithAppleResponses, AuthWithAppleErrors, ThrowOnError>({
    url: '/auth/apple',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Login
 *
 * Authenticate with email (or username) and password, return tokens.
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>) => (options.client ?? client).post<LoginResponses, LoginErrors, ThrowOnError>({
    url: '/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh
 *
 * Exchange a valid refresh token for new access + refresh tokens.
 *
 * This implements token rotation — the old refresh token is revoked
 * and a new one is issued.
 */
export const refresh = <ThrowOnError extends boolean = false>(options: Options<RefreshData, ThrowOnError>) => (options.client ?? client).post<RefreshResponses, RefreshErrors, ThrowOnError>({
    url: '/auth/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Logout
 *
 * Revoke the provided refresh token (sign out).
 */
export const logout = <ThrowOnError extends boolean = false>(options: Options<LogoutData, ThrowOnError>) => (options.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/auth/logout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Check Username
 *
 * Check whether a username is available for the current user to claim.
 *
 * Validates format and length, then checks uniqueness (case-insensitive).
 * The caller's own current username is always reported available so a
 * pre-filled handle during onboarding doesn't read as "taken".
 */
export const checkUsername = <ThrowOnError extends boolean = false>(options: Options<CheckUsernameData, ThrowOnError>) => (options.client ?? client).get<CheckUsernameResponses, CheckUsernameErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/users/check-username',
    ...options
});

/**
 * Delete Account
 *
 * Delete the current user's account and all associated data.
 */
export const deleteAccount = <ThrowOnError extends boolean = false>(options?: Options<DeleteAccountData, ThrowOnError>) => (options?.client ?? client).delete<DeleteAccountResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/users/me',
    ...options
});

/**
 * Get My Profile
 *
 * Get the current user's full profile.
 */
export const getMyProfile = <ThrowOnError extends boolean = false>(options?: Options<GetMyProfileData, ThrowOnError>) => (options?.client ?? client).get<GetMyProfileResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/users/me',
    ...options
});

/**
 * Update My Profile
 *
 * Update the current user's profile fields.
 */
export const updateMyProfile = <ThrowOnError extends boolean = false>(options: Options<UpdateMyProfileData, ThrowOnError>) => (options.client ?? client).patch<UpdateMyProfileResponses, UpdateMyProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/users/me',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Search Users
 *
 * Search users by username (prefix match).
 */
export const searchUsers = <ThrowOnError extends boolean = false>(options: Options<SearchUsersData, ThrowOnError>) => (options.client ?? client).get<SearchUsersResponses, SearchUsersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/users/search',
    ...options
});

/**
 * Get User Profile
 *
 * Get another user's public profile.
 */
export const getUserProfile = <ThrowOnError extends boolean = false>(options: Options<GetUserProfileData, ThrowOnError>) => (options.client ?? client).get<GetUserProfileResponses, GetUserProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/users/{user_id}',
    ...options
});

/**
 * List Cars
 *
 * List all cars in the current user's garage.
 */
export const listCars = <ThrowOnError extends boolean = false>(options?: Options<ListCarsData, ThrowOnError>) => (options?.client ?? client).get<ListCarsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cars',
    ...options
});

/**
 * Create Car
 *
 * Add a new car to the garage.
 *
 * If this is the user's first car, it becomes the active car automatically.
 */
export const createCar = <ThrowOnError extends boolean = false>(options: Options<CreateCarData, ThrowOnError>) => (options.client ?? client).post<CreateCarResponses, CreateCarErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cars',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Car
 *
 * Remove a car from the garage.
 *
 * Cannot delete the last car — users must always have at least one.
 * If deleting the active car, another car is auto-activated.
 */
export const deleteCar = <ThrowOnError extends boolean = false>(options: Options<DeleteCarData, ThrowOnError>) => (options.client ?? client).delete<DeleteCarResponses, DeleteCarErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cars/{car_id}',
    ...options
});

/**
 * Update Car
 *
 * Update a car's details.
 */
export const updateCar = <ThrowOnError extends boolean = false>(options: Options<UpdateCarData, ThrowOnError>) => (options.client ?? client).patch<UpdateCarResponses, UpdateCarErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cars/{car_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Activate Car
 *
 * Set a car as the active car (shown on map and in profile).
 */
export const activateCar = <ThrowOnError extends boolean = false>(options: Options<ActivateCarData, ThrowOnError>) => (options.client ?? client).post<ActivateCarResponses, ActivateCarErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cars/{car_id}/activate',
    ...options
});

/**
 * List Friends
 *
 * List all accepted friends.
 */
export const listFriends = <ThrowOnError extends boolean = false>(options?: Options<ListFriendsData, ThrowOnError>) => (options?.client ?? client).get<ListFriendsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/friends',
    ...options
});

/**
 * List Friend Requests
 *
 * List pending incoming friend requests.
 */
export const listFriendRequests = <ThrowOnError extends boolean = false>(options?: Options<ListFriendRequestsData, ThrowOnError>) => (options?.client ?? client).get<ListFriendRequestsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/friends/requests',
    ...options
});

/**
 * Send Friend Request
 *
 * Send a friend request to another user.
 */
export const sendFriendRequest = <ThrowOnError extends boolean = false>(options: Options<SendFriendRequestData, ThrowOnError>) => (options.client ?? client).post<SendFriendRequestResponses, SendFriendRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/friends/request',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Accept Friend Request
 *
 * Accept an incoming friend request.
 */
export const acceptFriendRequest = <ThrowOnError extends boolean = false>(options: Options<AcceptFriendRequestData, ThrowOnError>) => (options.client ?? client).post<AcceptFriendRequestResponses, AcceptFriendRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/friends/accept',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Decline Friend Request
 *
 * Decline an incoming friend request.
 */
export const declineFriendRequest = <ThrowOnError extends boolean = false>(options: Options<DeclineFriendRequestData, ThrowOnError>) => (options.client ?? client).post<DeclineFriendRequestResponses, DeclineFriendRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/friends/decline',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove Friend
 *
 * Remove a friend (unfriend).
 */
export const removeFriend = <ThrowOnError extends boolean = false>(options: Options<RemoveFriendData, ThrowOnError>) => (options.client ?? client).delete<RemoveFriendResponses, RemoveFriendErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/friends/{user_id}',
    ...options
});

/**
 * Create Convoy
 *
 * Create a new convoy. The creator becomes the leader.
 */
export const createConvoy = <ThrowOnError extends boolean = false>(options: Options<CreateConvoyData, ThrowOnError>) => (options.client ?? client).post<CreateConvoyResponses, CreateConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Convoy
 *
 * Get convoy details with member list.
 */
export const getConvoy = <ThrowOnError extends boolean = false>(options: Options<GetConvoyData, ThrowOnError>) => (options.client ?? client).get<GetConvoyResponses, GetConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}',
    ...options
});

/**
 * Update Convoy
 *
 * Update convoy settings (leader only).
 */
export const updateConvoy = <ThrowOnError extends boolean = false>(options: Options<UpdateConvoyData, ThrowOnError>) => (options.client ?? client).patch<UpdateConvoyResponses, UpdateConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * End Convoy
 *
 * End the convoy (leader only). Dissolves it for everyone.
 */
export const endConvoy = <ThrowOnError extends boolean = false>(options: Options<EndConvoyData, ThrowOnError>) => (options.client ?? client).post<EndConvoyResponses, EndConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/end',
    ...options
});

/**
 * Join Convoy
 *
 * Join a convoy directly (friend's convoy or accepted invite).
 */
export const joinConvoy = <ThrowOnError extends boolean = false>(options: Options<JoinConvoyData, ThrowOnError>) => (options.client ?? client).post<JoinConvoyResponses, JoinConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/join',
    ...options
});

/**
 * Request To Join
 *
 * Request to join a public convoy.
 */
export const requestToJoin = <ThrowOnError extends boolean = false>(options: Options<RequestToJoinData, ThrowOnError>) => (options.client ?? client).post<RequestToJoinResponses, RequestToJoinErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/request',
    ...options
});

/**
 * Accept Join Request
 *
 * Accept a join request (leader only).
 */
export const acceptJoinRequest = <ThrowOnError extends boolean = false>(options: Options<AcceptJoinRequestData, ThrowOnError>) => (options.client ?? client).post<AcceptJoinRequestResponses, AcceptJoinRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/request/{request_id}/accept',
    ...options
});

/**
 * Decline Join Request
 *
 * Decline a join request (leader only).
 */
export const declineJoinRequest = <ThrowOnError extends boolean = false>(options: Options<DeclineJoinRequestData, ThrowOnError>) => (options.client ?? client).post<DeclineJoinRequestResponses, DeclineJoinRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/request/{request_id}/decline',
    ...options
});

/**
 * Leave Convoy
 *
 * Leave the convoy.
 */
export const leaveConvoy = <ThrowOnError extends boolean = false>(options: Options<LeaveConvoyData, ThrowOnError>) => (options.client ?? client).post<LeaveConvoyResponses, LeaveConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/leave',
    ...options
});

/**
 * Invite To Convoy
 *
 * Invite a user to the convoy (any member can invite).
 */
export const inviteToConvoy = <ThrowOnError extends boolean = false>(options: Options<InviteToConvoyData, ThrowOnError>) => (options.client ?? client).post<InviteToConvoyResponses, InviteToConvoyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/invite',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Kick Member
 *
 * Remove a member from the convoy (leader only).
 */
export const kickMember = <ThrowOnError extends boolean = false>(options: Options<KickMemberData, ThrowOnError>) => (options.client ?? client).post<KickMemberResponses, KickMemberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/kick',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Messages
 *
 * Get recent messages in the convoy chat (paginated, newest last).
 */
export const getMessages = <ThrowOnError extends boolean = false>(options: Options<GetMessagesData, ThrowOnError>) => (options.client ?? client).get<GetMessagesResponses, GetMessagesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/messages',
    ...options
});

/**
 * Send Message
 *
 * Send a text message in the convoy chat.
 */
export const sendMessage = <ThrowOnError extends boolean = false>(options: Options<SendMessageData, ThrowOnError>) => (options.client ?? client).post<SendMessageResponses, SendMessageErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/messages',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send Quick Action
 *
 * Send a quick action alert to the convoy.
 */
export const sendQuickAction = <ThrowOnError extends boolean = false>(options: Options<SendQuickActionData, ThrowOnError>) => (options.client ?? client).post<SendQuickActionResponses, SendQuickActionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/quick-action',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Active Route
 *
 * Get the current active route for the convoy.
 */
export const getActiveRoute = <ThrowOnError extends boolean = false>(options: Options<GetActiveRouteData, ThrowOnError>) => (options.client ?? client).get<GetActiveRouteResponses, GetActiveRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/route',
    ...options
});

/**
 * Set Route
 *
 * Set a group route for the convoy (any member can set).
 */
export const setRoute = <ThrowOnError extends boolean = false>(options: Options<SetRouteData, ThrowOnError>) => (options.client ?? client).post<SetRouteResponses, SetRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/convoys/{convoy_id}/route',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Spatial Config
 *
 * Return spatial system configuration for clients.
 *
 * Clients use this to know which H3 resolutions are supported
 * and should be used for viewport cell computation.
 */
export const getSpatialConfig = <ThrowOnError extends boolean = false>(options?: Options<GetSpatialConfigData, ThrowOnError>) => (options?.client ?? client).get<GetSpatialConfigResponses, unknown, ThrowOnError>({ url: '/spatial/config', ...options });

/**
 * Debug Spatial Store
 *
 * Dump the full state of the in-memory spatial store. Only available when DEBUG=true.
 */
export const debugSpatialStore = <ThrowOnError extends boolean = false>(options?: Options<DebugSpatialStoreData, ThrowOnError>) => (options?.client ?? client).get<DebugSpatialStoreResponses, unknown, ThrowOnError>({ url: '/spatial/debug', ...options });

/**
 * Health Check
 *
 * Basic health check endpoint.
 */
export const healthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthCheckData, ThrowOnError>) => (options?.client ?? client).get<HealthCheckResponses, unknown, ThrowOnError>({ url: '/health', ...options });
