---
phase: 01-auth-onboarding
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - SlipStream/Onboarding/OnboardingContainerView.swift
  - SlipStream/Auth/AuthState.swift
  - SlipStream/Components/ToastView.swift
  - SlipStream/Services/APIClient.swift
  - backend/src/backend/models.py
  - backend/src/backend/routers/cars.py
autonomous: true
requirements:
  - ONBOARD-04
  - AUTH-03

must_haves:
  truths:
    - "User cannot access map until at least one car exists in garage"
    - "Onboarding flow saves username via PATCH /users/me"
    - "Onboarding flow creates car via POST /cars"
    - "Token refresh happens silently when access token expires"
    - "Error states show toast notifications that can be dismissed"
    - "Completing onboarding transitions user to map screen"
  artifacts:
    - path: "SlipStream/Components/ToastView.swift"
      provides: "Error/success notification component"
      contains: "struct ToastView"
    - path: "SlipStream/Onboarding/OnboardingContainerView.swift"
      provides: "Full onboarding flow with data persistence"
      contains: "saveUsername"
  key_links:
    - from: "SlipStream/Onboarding/OnboardingContainerView.swift"
      to: "SlipStream/Services/APIClient.swift"
      via: "PATCH /users/me for username"
      pattern: "apiClient.request.*users/me.*PATCH"
    - from: "SlipStream/Auth/AuthState.swift"
      to: "SlipStream/Services/AuthService.swift"
      via: "token refresh"
      pattern: "authService.refreshTokens"
---

<objective>
Wire together the complete onboarding flow: username saved to backend, car created, error handling with toast notifications, and smooth transition to map on completion.

Purpose: This plan completes Phase 1 by ensuring all onboarding data persists and the user can access the app.

Output: Complete end-to-end auth and onboarding flow with error handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-onboarding/SKELETON.md
@.planning/phases/01-auth-onboarding/01-CONTEXT.md (D-09, D-11, D-12)
@.planning/phases/01-auth-onboarding/01-UI-SPEC.md (Toast specs, error copy)

<interfaces>
<!-- Key types from prior plans -->

From SlipStream/Services/APIClient.swift:
- func request<T: Decodable>(_ endpoint: String, method: String, body: Encodable?, authenticated: Bool) async throws -> T

From SlipStream/Auth/AuthState.swift:
- func completeOnboarding() — transitions to .authenticated

From backend/src/backend/routers/users.py:
- PATCH /users/me with UpdateProfileRequest: username, display_name, etc.
- Returns UserProfile

From backend/src/backend/routers/cars.py:
- POST /cars with CreateCarRequest: year, make, model, trim?, color, photo_url?, mods?
- First car is auto-activated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add display_name field to Car model and endpoint</name>
  <files>backend/src/backend/models.py, backend/src/backend/routers/cars.py</files>
  <read_first>
    - backend/src/backend/models.py (Car model definition)
    - backend/src/backend/routers/cars.py (CreateCarRequest, CarResponse schemas)
    - .planning/phases/01-auth-onboarding/01-CONTEXT.md (D-06)
  </read_first>
  <action>
Update backend to support car display_name per D-06.

**backend/src/backend/models.py**: Add display_name field to Car model:
- display_name: Mapped[str | None] = mapped_column(Text, nullable=True)

**backend/src/backend/routers/cars.py**: 
- Add display_name to CreateCarRequest: display_name: str | None = Field(default=None, max_length=50)
- Add display_name to UpdateCarRequest: display_name: str | None = Field(default=None, max_length=50)
- Add display_name to CarResponse: display_name: str | None = None
- Update _car_to_response helper to include display_name
- Update create_car route to set car.display_name = body.display_name

Create Alembic migration for the new column:
- cd backend && uv run alembic revision --autogenerate -m "add_car_display_name"
- Review generated migration to ensure it adds nullable column

Per D-06: Display name decouples database identity (year/make/model) from how the car appears on map/profile.
  </action>
  <verify>
    <automated>grep -c "display_name" /home/kaibreese/slipstream/backend/src/backend/routers/cars.py returns at least 3</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/backend/models.py Car model contains display_name: Mapped[str | None]
    - backend/src/backend/routers/cars.py CreateCarRequest contains display_name field
    - backend/src/backend/routers/cars.py CarResponse contains display_name field
    - backend/src/backend/routers/cars.py _car_to_response includes display_name
    - Alembic migration file created in backend/alembic/versions/
  </acceptance_criteria>
  <done>Car model and endpoints support display_name field for enthusiast identity</done>
</task>

<task type="auto">
  <name>Task 2: Create ToastView component for error notifications</name>
  <files>SlipStream/Components/ToastView.swift</files>
  <read_first>
    - SlipStream/SlipStreamStyle.swift (design tokens)
    - .planning/phases/01-auth-onboarding/01-UI-SPEC.md (Toast specs, animation)
  </read_first>
  <action>
Create **SlipStream/Components/ToastView.swift**: Notification overlay component.

Define ToastStyle enum: error, success, info with associated colors (error: systemRed, success: systemGreen, info: accent).

**ToastView** struct properties:
- let message: String
- let style: ToastStyle
- var onDismiss: (() -> Void)? = nil

Body structure (per UI-SPEC):
- HStack with 12pt spacing
- Icon based on style: exclamationmark.circle for error, checkmark.circle for success, info.circle for info
- Text message at 14pt regular, max 2 lines
- Spacer
- X button to dismiss (if onDismiss provided)
- Padding 16pt horizontal, 12pt vertical
- Background: style color at 0.15 opacity with rounded corners (10pt)
- Border: style color at 0.3 opacity
- Max width 340pt, centered

**ToastModifier** ViewModifier for easy attachment:
- @Binding var isPresented: Bool
- let message: String
- let style: ToastStyle
- let duration: Double (default 3.0)

Creates overlay at top of screen with ToastView. Animates in (slide down, 250ms spring per UI-SPEC), auto-dismisses after duration (200ms easeIn fade out). Can be dismissed by tap.

Extension on View:
- func toast(isPresented: Binding<Bool>, message: String, style: ToastStyle, duration: Double = 3.0) -> some View

Per D-11: Auth failures show toast + stay on sign-in screen.
Per D-12: Photo upload failures show retry + skip (toast is just the notification part).
  </action>
  <verify>
    <automated>grep -c "struct ToastView" /home/kaibreese/slipstream/SlipStream/Components/ToastView.swift returns 1</automated>
  </verify>
  <acceptance_criteria>
    - SlipStream/Components/ToastView.swift contains struct ToastView
    - SlipStream/Components/ToastView.swift contains enum ToastStyle with error, success, info cases
    - SlipStream/Components/ToastView.swift contains ToastModifier ViewModifier
    - ToastView has HStack with icon, message, optional dismiss button
    - Animations match UI-SPEC: 250ms spring in, 200ms easeIn out
    - View extension .toast() modifier available
    - Auto-dismiss after configurable duration (default 3 seconds)
  </acceptance_criteria>
  <done>ToastView component with error/success/info styles, animation, and auto-dismiss</done>
</task>

<task type="auto">
  <name>Task 3: Wire onboarding data persistence and completion flow</name>
  <files>SlipStream/Onboarding/OnboardingContainerView.swift, SlipStream/Auth/AuthState.swift, SlipStream/Services/APIClient.swift</files>
  <read_first>
    - SlipStream/Onboarding/OnboardingContainerView.swift (created in Plan 02)
    - SlipStream/Onboarding/UsernameStepView.swift (username binding)
    - SlipStream/Onboarding/CarStepView.swift (car creation)
    - SlipStream/Auth/AuthState.swift (state management)
    - .planning/phases/01-auth-onboarding/01-CONTEXT.md (D-09)
  </read_first>
  <action>
Wire the complete onboarding flow with data persistence.

**OnboardingContainerView.swift** updates:
- Add @State private var showToast = false
- Add @State private var toastMessage = ""
- Add @State private var toastStyle: ToastStyle = .error
- Add @State private var isSubmitting = false
- Add private let apiClient = APIClient()

Update UsernameStepView callback to capture username in container state (already done via binding).

Update PhotoStepView in pageView(for:) to pass profileImage binding.

Update CarStepView in pageView(for:):
- onComplete callback should:
  1. Set isSubmitting = true
  2. Call saveUsername() to PATCH /users/me with username
  3. Car creation already happens in CarStepView
  4. On success: call authState.completeOnboarding()
  5. On error: show toast with error message, set isSubmitting = false

Add func saveUsername() async throws:
- Build UpdateProfileRequest with username field
- PATCH /users/me via apiClient
- No return value needed, just needs to succeed

Add .toast(isPresented: $showToast, message: toastMessage, style: toastStyle) modifier to container.

**AuthState.swift** updates:
- Ensure completeOnboarding() transitions state to .authenticated
- Add token refresh logic to checkAuthStatus():
  - If tokens exist but access token is expired, call authService.refreshTokens()
  - If refresh succeeds, proceed to validate with /users/me
  - If refresh fails, clear tokens and set state to .unauthenticated

**APIClient.swift** updates (if not already done):
- Ensure 401 response triggers token refresh attempt
- If refresh succeeds, retry original request
- If refresh fails, throw AuthenticationError

Per D-09: Silent token refresh when app resumes.
Per ONBOARD-04: User cannot access app until car exists (enforced by onboarding flow requiring car step completion).
  </action>
  <verify>
    <automated>grep -c "saveUsername" /home/kaibreese/slipstream/SlipStream/Onboarding/OnboardingContainerView.swift returns at least 1</automated>
  </verify>
  <acceptance_criteria>
    - SlipStream/Onboarding/OnboardingContainerView.swift contains func saveUsername() async throws
    - SlipStream/Onboarding/OnboardingContainerView.swift has .toast() modifier attached
    - SlipStream/Onboarding/OnboardingContainerView.swift CarStepView onComplete saves username then completes onboarding
    - SlipStream/Auth/AuthState.swift completeOnboarding() sets state = .authenticated
    - SlipStream/Auth/AuthState.swift checkAuthStatus() handles token refresh
    - SlipStream/Services/APIClient.swift retries request after successful token refresh on 401
    - Error states show toast notification
    - Successful completion transitions to map screen
  </acceptance_criteria>
  <done>Complete onboarding flow: username saved to backend, car created, errors show toast, completion transitions to map</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS App → Backend | All data persistence requires authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Username change | mitigate | Backend validates username uniqueness on PATCH |
| T-04-02 | Repudiation | Onboarding completion | accept | Completion state derived from having car, not a separate flag |
| T-04-03 | Elevation of Privilege | Skip car requirement | mitigate | Onboarding flow requires CarStepView onComplete; cannot skip |
</threat_model>

<verification>
Phase 1 Plan 04 complete when:
- display_name field available in POST /cars
- ToastView component displays error/success notifications
- Username is persisted to backend on onboarding completion
- Token refresh works silently (access token expires, refresh succeeds, user stays authenticated)
- Completing onboarding transitions to map screen
- User with car can see map; user without car cannot bypass onboarding
</verification>

<success_criteria>
1. POST /cars accepts display_name field
2. Alembic migration for display_name column created
3. ToastView displays with correct styling and auto-dismisses
4. Onboarding completion PATCHes username to /users/me
5. Error during save shows toast, does not crash
6. Successful save transitions state to .authenticated
7. Map screen visible after onboarding completion
8. Token refresh works when access token expired
</success_criteria>

<output>
Create `.planning/phases/01-auth-onboarding/01-04-SUMMARY.md` when done
</output>
